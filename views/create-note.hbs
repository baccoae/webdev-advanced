<h1 class="mb-4">Create your own <span class="bg-primary text-italic text-light px-2">Note</span></h1>
{{#if validationErrors.length}}
    <div class="toast toast-error mb-4">
        <p>Could not create a new note because of the following errors : </p>
        <ul>
            {{#each validationErrors}}
                <li>{{this}}</li>
            {{/each}}
        </ul>
    </div>
{{/if}}
<div class="container mb-6">
    <div class="columns">
        <div class="column col-1"></div>
        <div class="column col-6 mr-4">
            <form action="" method="post">
                <div class="form-group mt-3">
                    <label for="Note title" class="text-small form-label">What should your note be called ?</label>
                    <input type="text" name="title" class="form-input" placeholder="Title" value="{{noteTitle}}" />
                </div>

                <div class="form-group mt-3">
                    <label for="Note title" class="text-small form-label">Paste a picture link from
                        <a
                            class="link tooltip"
                            data-tooltip="Right click on an image → Copy image link"
                            target="_blank"
                            href="https://www.unsplash.com"
                        >Unsplash.com</a></label>
                    <div class="input-group">
                        <span class="input-group-addon addon-md">www.unsplash.com/</span>
                        <input type="text" name="src" class="form-input input-lg" placeholder="Image link" value="{{noteImage}}" />
                    </div>
                </div>

                <div class="form-group mt-3">
                    <label for="Note title" class="text-small form-label">Describe your notes in a few words
                        <span class="text-gray">(120 characters max.) :</span></label>
                    <textarea type="Textarea" name="desc" maxlength="120" class="form-input" placeholder="Description">{{noteDesc}}</textarea>
                </div>

                <input type="submit" class="btn btn-primary px-3" value="Create note" />
            </form>
        </div>
        <div class="column col-4">
            <img src="/images/undraw_add_notes.svg" alt="" class="img-responsive ml-4" />
        </div>
    </div>
</div>